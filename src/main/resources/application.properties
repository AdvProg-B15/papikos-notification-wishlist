spring.application.name=notification-wishlist

# Server Configuration
server.port=8080

# --- JPA & Hibernate Configuration ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# ===================================================================
# AMQP CONFIGURATION
# ===================================================================
spring.rabbitmq.host=armadillo.rmq.cloudamqp.com
spring.rabbitmq.port=5671
spring.rabbitmq.username=vucjkybv
spring.rabbitmq.password=vNxvaQMfgwYYE983cE5QTt7tgUdkUdjU
spring.rabbitmq.virtual-host=vucjkyb
# ===================================================================
# AUTH CONFIGURATION
# ===================================================================
# Use AUTH_SERVICE_URL as per your env var
auth.service.url=${AUTH_SERVICE_URL:http://localhost:8081}
kos.service.url=${KOS_SERVICE_URL:http://localhost:8082}
rental.service.url=${RENTAL_SERVICE_URL:http://localhost:8083}
# ===================================================================
# LOGGING CONFIGURATION
# ===================================================================
logging.level.org.springframework.boot.autoconfigure.jdbc=DEBUG
logging.level.org.springframework.boot.docker.compose=DEBUG
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ===================================================================
# DATABASE CONFIGURATION
# ===================================================================
# Ensure DB_NAME default matches your env var if it was intended to be the primary source
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:postgres}
# Use environment variables for username and password, with fallbacks
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

internal.token.secret=${INTERNAL_TOKEN_SECRET:your-very-secure-and-long-internal-token-secret}

logging.level.id.ac.ui.cs.advprog.papikos.notification.client.KosServiceClient=DEBUG

